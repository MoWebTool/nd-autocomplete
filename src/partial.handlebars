<div class="{{classPrefix}}-content">
  <ul data-role="items">
  {{#each items}}
    <li data-role="item" class="{{../classPrefix}}-item"  title="{{label}}">{{{label}}}</li>
  {{/each}}
  </ul>
</div>
